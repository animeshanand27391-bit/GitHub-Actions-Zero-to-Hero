name: Python Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted   # ← was missing this!

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: pip3 install pytest

      - name: Run tests
        run: |
          cd src
          python3 -m pytest addition.py